<?php

include '../utils/token.php';

$token =  "";

echo json_encode(["message" => "No tests"]);
// echo Token::getHeader($token);
// echo json_decode(Token::getPayload($token), true)["access_level"];
